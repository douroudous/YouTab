<h2> <%= @song.title %> </h2>
<h3> <%= link_to @song.artist.name, @song.artist %> </h3>
<h3> <%= link_to @song.user.username, @song.user %> </h3>

<div>
  <p><%= link_to "Update Tab", edit_song_path %></p>
  <p><%= link_to "Delete Tab", @song, method: :delete %></p>
</div>
