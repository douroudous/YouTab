<h2>Edit Artist Form</h2>

<%= form_for(@artist) do |f| %>
  <div class="inline-block form-text">
    <%= f.label :name, class: "form-label" %>
    <%= f.text_field :name %>
    <%= f.submit "Update Artist"%>
  </div>
<% end %>
